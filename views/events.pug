extends layout

block content
  div(id='main')
    div(class='inner')

      header(id='header')
        h1= title       

      section(id='banner')
        div(class='content')
          header
            
          table
              tr
                td Nom
                td Type
                td Date
                td Durée (en jours)
                td Détails
                td Demande(s)
              each event, i in eventlist
                tr
                  td
                    = event.name
                  td
                    = event.type
                  td
                    = event.date
                  td
                    = event.days
                  td
                    a(href='/events/'+event._id)  ...
                  //td
                    a(href='/events/'+event._id+'/postul') ...